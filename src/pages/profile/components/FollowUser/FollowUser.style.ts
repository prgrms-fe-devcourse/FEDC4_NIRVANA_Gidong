import styled from '@emotion/styled';

export const FollowUserLayout = styled.div`
  display: flex;
  align-items: center;
  & > div {
    margin-right: 15px;
  }

  & > span:last-child {
    margin-left: 2px;
  }

  & .dot {
    bottom: 1px;
    right: 1px;
  }
`;
